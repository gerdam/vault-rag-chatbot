server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # SPA-Fallback: unbekannte Pfade auf index.html, damit Client-Routing funktioniert.
        try_files $uri $uri/ /index.html;
    }
}
